export interface ListBook {
  title: string;
  authors: string;
  publisher: string;
  rating: number;
  count: number;
  image: string;
  isbn: string;
}

export type TopSwiperBook = {
  image: string;
  isbn: string;
}
